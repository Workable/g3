{
    "blocks": [

        {
            "type": "section",
            "text": {
                "type": "mrkdwn",
                "text": "G3 CI/PR-${{ env.pr_number }}: ${{ env.current_job_status }}\n```Time   : ${{ env.overall_time }}\nBranch : ${{ env.branch }}\nPR     : ${{ env.pr_url }}\nDiff   : ${{ env.diff}}\n\n    ${{ env.commit_message }}```"
            }
        }
    ]
}
